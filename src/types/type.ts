export type Credentials = {
  username: string
  password: string
}

export type User = {
  id: number
  firstName: string
  lastName: string
  username: string
  registerDate: string
  role: string
}